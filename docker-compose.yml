version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: lapata_postgres
    environment:
      POSTGRES_DB: missing_persons_db
      POSTGRES_USER: dracx
      POSTGRES_PASSWORD: ksn8866
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  postgres_data: